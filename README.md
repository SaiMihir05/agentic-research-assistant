# Agentic Research Assistant

A **production‑grade** AI research assistant built with FastAPI, PostgreSQL, Docker, and LangChain.  
The project is intentionally built **incrementally**, mimicking a real‑world engineering timeline.  
Each day you’ll get a concrete coding milestone, a natural‑sounding Git commit, and a short developer journal.

## Quick Start (Day 1‑5)
```bash
# Clone the repo and cd into the folder
git clone <repo‑url>
cd agentic-research-assistant

# Create a virtual environment (optional but recommended)
python -m venv .venv
source .venv/bin/activate  # on Windows: .venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Run the containers (Postgres + Redis)
 docker compose -f docker-compose.yml up -d

# Start the API (auto‑reload enabled)
uvicorn app.main:app --reload
```

Open <http://localhost:8000/docs> to explore the autogenerated OpenAPI UI.

---

### Why this project?
- **Showcase**: A full‑stack, AI‑driven backend you can point to on a resume.
- **Learning**: Hands‑on experience with async FastAPI, migrations, LLM agents, and production ops.
- **Extensible**: Later phases add LangGraph orchestration, Redis caching, Qdrant vector search, and AWS deployment.

---

## Project Structure
```
agentic-research-assistant/
├─ app/                # FastAPI app, settings, routes, models
├─ alembic/            # DB migrations
├─ docs/               # Architecture, setup, decisions docs
├─ tests/              # Pytest suite
├─ Dockerfile          # Container image for the API
├─ docker-compose.yml  # Services: api, postgres, redis
├─ pyproject.toml      # Build metadata (optional)
├─ requirements.txt    # Pinned deps
└─ README.md           # You’re reading it!
```

---

## Next Steps
- **Day 2**: Docker + PostgreSQL integration
- **Day 3**: User model & Alembic migrations
- **Day 4**: Query model & repository pattern
- **Day 5**: Logging, error handling, test scaffolding

Happy coding! 🚀
